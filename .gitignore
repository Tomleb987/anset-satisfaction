.vercel
node_modules/
package-lock.json

# Réglages locaux de l'agent (permissions machine) — pas de partage
.claude/settings.local.json

# État local CLI Supabase
supabase/.temp/

# Données clients (RGPD) — NE JAMAIS committer les exports .xlsx
*.xlsx
